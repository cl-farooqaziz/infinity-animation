// Meta Data
export const metadata = {
    // Title & Descriptions
    title: "Infinity Animation - Infographics Design",
    description: "Infinity Animation",
}

export default function RootLayout({ children }) {
    return (children);
}