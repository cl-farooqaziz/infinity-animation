// Meta Data
export const metadata = {
    // Title & Descriptions
    title: "Infinity Animation - Architectural Services ",
    description: "Infinity Animation",
}

export default function RootLayout({ children }) {
    return (children);
}