// Meta Data
export const metadata = {
    // Title & Descriptions
    title: "Infinity Animation - Portfolio",
    description: "Infinity Animation",
}

export default function RootLayout({ children }) {
    return (children);
}